[**@ekim088/toolkit**](../README.md)

---

[@ekim088/toolkit](../README.md) / chunk

# Function: chunk()

> **chunk**\<`T`\>(`array`, `chunkSize`): `T`[][]

Defined in: [chunk.ts:8](https://github.com/ekim088/toolkit/blob/main/src/chunk.ts#L8)

Chunks an array into sub-arrays of at most `chunkSize` items.

## Type Parameters

### T

`T`

## Parameters

### array

readonly `T`[]

The array to chunk.

### chunkSize

`number`

The max number of items per chunk.

## Returns

`T`[][]

The chunked array.
